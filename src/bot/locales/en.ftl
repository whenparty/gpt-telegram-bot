select-model = Select the gpt model
# $aiModel (String) - The name of the gpt model
no-tokens-error = Unfortunately you do not have tokens for { $aiModel }
chat-cleared = Chat history cleared
not-limited = Not limited
# $modelDisplayName (String) - The display name of the gpt model
# $tokenAmount (String) - The amount of tokens for the gpt model
set-ai-model-option = { $modelDisplayName } / tokens: { $tokenAmount }
